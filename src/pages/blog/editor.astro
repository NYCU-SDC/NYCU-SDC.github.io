---
import Layout from "@/layouts/Layout.astro";
import BlogEditor from "@/components/editor/BlogEditor";
---
<Layout hideNav={true}>
	<section>
		<BlogEditor client:load />
	</section>
	<script>
		// import { initEditor } from "@/scripts/initEditor.ts";
        // import { processMarkdownImage } from "@/scripts/imageUtil.ts";
        // import { saveDraft } from "@/lib/request/saveDraft.ts";
		// import { Crepe } from "@milkdown/crepe";
        // import { createDraft } from "@/lib/request/createDraft.ts";
        // import { getDrafts } from "@/lib/request/getDrafts.ts";
        // import type { Draft } from "@/types/draft.ts";

        // const blogsList = document.querySelector<HTMLDivElement>(".editor-blog-list")!!;
        // const createDraftButton = document.querySelector<HTMLButtonElement>(".new-post")!!;

		// const uploadButton = document.querySelector<HTMLButtonElement>("#upload")!!;
		// const previewImage = document.querySelector<HTMLImageElement>("#preview")!!
        //     		const fileInput = document.querySelector<HTMLInputElement>("#image")!!;
            
        // const titleInput = document.querySelector<HTMLInputElement>(".editor-title-input")!!;    
		// const editor = document.querySelector<HTMLElement>("#editor")!!;
        // const saveButton = document.querySelector<HTMLButtonElement>("#save-button")!!;
        // const loginButton = document.querySelector<HTMLButtonElement>("#login-button")!!;

        // const blogs: Array<Draft> = [];

		// let selectedImage;
        // let crepe: Crepe;

		// if (editor) {
		// 	crepe = await initEditor(editor);
		// }

        // const refreshBlogList = async () => {
        //     blogs.splice(0, blogs.length, ...(await getDrafts() as Array<Draft>));
        //     console.log("refreshing blog list", blogs);
        //     blogsList.innerHTML = "";
        //     blogs.forEach((blog) => {
        //         const blogItem = document.createElement("div");
        //         blogItem.textContent = blog.title;
        //         blogItem.className = "post-button";
        //         blogsList.appendChild(blogItem);
        //     });
        //     const newPostButton = document.createElement("div");
        //     newPostButton.textContent = "新增草稿";
        //     newPostButton.className = "new-post post-button";
        //     newPostButton.onclick = () => {
        //         createDraft().then((draft) => {
        //             blogs.push(draft);
        //             refreshBlogList();
        //         });
        //     };
        //     blogsList.appendChild(newPostButton);
        //     console.log("refreshed blog list", blogs);
        // }

		// uploadButton.addEventListener("click", () => {
		// 	fileInput.click();
		// });

		// fileInput.addEventListener("change", e => {
		// 	const target = e.target as HTMLInputElement;
		// 	const file = target.files?.[0];
		// 	if (!file) return;
		// 	selectedImage = file;

		// 	if (selectedImage) {
		// 		const url = URL.createObjectURL(selectedImage);
		// 		previewImage.src = url;
		// 	}
		// });

        // saveButton.addEventListener("click", async () => {
        //     if (!crepe) return;
            
        //     const rawContent = crepe.getMarkdown();
        //     const parsedContent = await processMarkdownImage(rawContent, "draft");
        //     console.log(parsedContent);
        //     crepe.setMarkdown(parsedContent);

        //     saveDraft(parsedContent.id, titleInput.value, parsedContent.content);
        // });

        // loginButton.addEventListener("click", () => {
        //     window.location.href = `${import.meta.env.PUBLIC_BACKEND_BASE_URL}/auth/login?r=${window.location.href}`;
        // });

        // createDraftButton.addEventListener("click", () => {
        //     createDraft().then((draft) => {
        //         blogs.push(draft);
        //     });
        // });

        // refreshBlogList();
	</script>
    <style>
        body {
            background-color: var(--nord0);
            color: var(--nord6);
        }
        section {
            display: flex;
            padding: 2rem 1rem;
            justify-content: center;
            gap: 2rem;
            height: 100dvh;
        }
    </style>
</Layout>
