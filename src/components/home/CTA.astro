<section>
	<div class="cta-card">
		<h2>準備好加入我們了嗎？</h2>
		<p class="cta-description">
			成為交大軟體開發社的一員，<br />開始你的軟體開發、AI 與專案實作之旅
		</p>
		<a href="https://url.sdc.nycu.club/registration" class="cta-button" target="_blank" rel="noopener noreferrer"> 立即加入 </a>
	</div>
</section>

<style>
	section {
		max-width: var(--max-width);
		margin: 0 auto;
		padding: 2rem;
	}

	.cta-card {
		background: var(--nord6);
		border-radius: 1rem;
		padding: 4rem 2rem;
		text-align: center;
		box-shadow: 0px 4px 9.2px 0px rgba(76, 86, 106, 0.16);
	}

	h2 {
		font-size: clamp(2rem, 5vw, 2.5rem);
		color: var(--nord1);
		margin-bottom: 1rem;
		font-weight: 600;
	}

	.cta-description {
		font-size: clamp(1rem, 3vw, 1.25rem);
		color: var(--nord3);
		margin-bottom: 2rem;
		line-height: 1.6;
		max-width: 600px;
		margin-left: auto;
		margin-right: auto;
	}

	.cta-button {
		display: inline-block;
		padding: 1rem 3rem;
		font-size: 1.25rem;
		font-weight: 600;
		color: var(--white);
		background: var(--primary);
		border-radius: 0.5rem;
		text-decoration: none;
		transition: all 0.2s ease;
		box-shadow: 0px 4px 9.2px 0px rgba(76, 86, 106, 0.16);
	}

	.cta-button:hover {
		opacity: 0.9;
		transform: translateY(-2px);
		box-shadow: 0px 6px 12px 0px rgba(76, 86, 106, 0.2);
	}

	.cta-button:active {
		transform: translateY(0);
	}

	@media (max-width: 50rem) {
		section {
			margin: 3rem auto;
		}

		.cta-card {
			padding: 3rem 1.5rem;
		}

		.cta-button {
			padding: 0.875rem 2.5rem;
			font-size: 1.1rem;
		}
	}
</style>
